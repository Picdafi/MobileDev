main(List<String> args) {
// int num=20;
 //assert (str != null);
// assert(num < 5,"false");

// assert(configFile != null, "Tool config missing.");

var test = false;
assert(test ? true : throw "message");

}